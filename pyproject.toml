[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cutting-board-local-services"
version = "0.1.0"
description = "IDE-independent local Services workspace for Ubuntu"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Cutting Board contributors" }]
keywords = ["developer-tools", "local-services", "ports", "process-monitor", "ubuntu"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: X11 Applications",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Topic :: Software Development",
  "Topic :: System :: Monitoring",
]
dependencies = ["psutil>=5.9,<8"]

[project.scripts]
cutting-board = "cutting_board.app:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 110

[tool.mypy]
python_version = "3.10"
strict = true
warn_unused_ignores = true
